#include "types.h"

// Reconstruction fragment for fn_80042864. Its eight-byte range is exact,
// but does not establish an original translation-unit boundary.

extern "C" s32 fn_80042864(void)
{
	return 1;
}
