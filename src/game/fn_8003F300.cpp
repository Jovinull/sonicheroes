#include "types.h"

// Reconstruction fragment for fn_8003F300. Its eight-byte range is exact,
// but does not establish an original translation-unit boundary.

extern "C" s32 fn_8003F300(void)
{
	return 0;
}
